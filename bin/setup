#!/usr/bin/env bash

set -e

bash ./bin/composer install --no-scripts

bash ./bin/npm install

bash ./bin/composer vendor-expose

sspak_file="$1"
if [ "$sspak_file" ]; then
  echo "loading $sspak_file..."
  sh ./bin/sspak load "$sspak_file" --drop-db
fi

bash ./bin/sake dev/build 'flush=1'
