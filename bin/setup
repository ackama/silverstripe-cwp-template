#!/usr/bin/env bash

set -e

sh ./bin/composer install --no-scripts

sh ./bin/npm install

sh ./bin/composer vendor-expose

sspak_file="$1"
if [ "$sspak_file" ]; then
  echo "loading $sspak_file..."
  sh ./bin/sspak load "$sspak_file" --drop-db
fi

sh ./bin/sake dev/build 'flush=1'
